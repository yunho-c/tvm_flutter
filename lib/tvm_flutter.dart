library tvm_flutter;

// This plugin provides the TVM C++ runtime for static/dynamic linking.
// It does not export any Dart APIs - it only ensures that libtvm_runtime
// and libtvm_ffi_static are linked into the final binary.
